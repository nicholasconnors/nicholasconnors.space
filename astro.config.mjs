// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://nicholasconnors.github.io',
    base: '/nicholasconnors.space'
});
